<t t-name="ViewMacro">
    <div class="position-fixed p-3" style="z-index: 100;">
        <t t-call="WidgetZoom"/>
        <t t-call="WidgetCurrentColor"/>
    </div>

    <div class="hv-center-fix p-3 bg-white" t-attf-style="zoom: {{zoom}}">
        <div class="macro_form">
            <div class="macro_dim">
                <t t-foreach="macro.quad_ids" t-as="quad" t-key="quad.id">
                    <!-- QUAD COLORATI + HEX CHE CAMBIANO COLORE -->
                    <div class="quad quad_dim bg-dark " t-att-style="getQuadStyle(quad)">
                        <t t-foreach="quad.hex_ids" t-as="hex" t-key="hex.id">
                            <div class="hex hex_dim hv-center"
                                 t-att-style="getHexStyle(hex)"
                                 t-att-class="currentColor == '' ? 'pointer': 'crosshair'"
                                 t-on-click="() => this.onClick(hex)"/>
                        </t>
                    </div>
                </t>
            </div>
        </div>
    </div>
</t>